# Development Requirements for SecureTranscribe
# These versions are specifically chosen for NumPy 1.x compatibility

# Core Dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.36
pydantic==2.10.5
pydantic-settings==2.3.5
python-multipart==0.0.9
aiofiles==24.1.0
python-dotenv==1.0.1
alembic==1.14.0
redis==5.1.1
celery==5.4.0
httpx==0.28.1
soundfile==0.13.1
librosa==0.10.2

# NumPy compatible ML dependencies
numpy==1.24.3  # CRITICAL: NumPy 1.x for spaCy compatibility
torch==2.3.1+cpu  # CPU version compatible with NumPy 1.x
torchaudio==2.3.1+cpu
faster-whisper==1.0.3

# Audio Processing
pyannote.audio==0.4.1
ffmpeg-python==0.3.0

# Data handling
pandas==2.2.3

# File Processing
python-magic==0.4.27
reportlab==4.0.7
openpyxl==3.1.2

# Utilities
jinja2==3.1.2

# Testing and Development
pytest==8.4.2
pytest-asyncio==0.23.8
pytest-cov==4.1.0
pytest-mock==3.14.0
black==24.1.1
flake8==7.0.0
mypy==1.10.1
pre-commit==3.7.1

# Type checking
types-python-date==2.9.0
types-redis==4.6.0.11
types-setuptools==70.1.0

# Security
python-jose[cryptography]==3.3.0
cryptography==44.0.0
passlib[bcrypt]==1.7.4

# System monitoring
psutil==6.1.0

# spaCy and Language Models (NumPy compatible)
spacy==3.8.2
en-core-web-sm==3.8.0

# GPU Support (optional - install separately if needed)
# torch==2.3.1  # For GPU, install with: pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cu118
# torchaudio==2.3.1

# Performance monitoring (optional)
# prometheus-client==0.19.0
# structlog==24.1.0

# NOTE: NumPy Compatibility Guidelines
# -----------------------------------
# 1. ALWAYS install numpy==1.24.3 BEFORE other ML packages
# 2. Use CPU versions of PyTorch for compatibility
# 3. spaCy 3.8.2 is tested with NumPy 1.24.3
# 4. Avoid packages compiled against NumPy 2.x
# 5. If GPU support needed, install torch separately after CPU versions

# Installation Order (IMPORTANT):
# pip install numpy==1.24.3
# pip install torch==2.3.1+cpu
# pip install -r requirements-dev.txt
# python -m spacy download en_core_web_sm
```
Now let me update the Dockerfile to handle NumPy compatibility properly:
